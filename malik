-- this file was generated by oxygen-Deobf
-- by 8n Dev
local loaderLib = loadstring(game:HttpGet("https://encrypt-x.pages.dev/Scripts?Id=8080934272573"))("8080934272573")
local uiLib = loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-mmnn-213506"))()

shared["LoaderTitle"] = "Malik Hub"
shared["LoaderKeyFrames"] = {
    {0.3, 10}, {0.3, 20}, {0.3, 30}, {0.3, 40}, {0.3, 50},
    {0.3, 60}, {0.3, 70}, {0.3, 80}, {0.3, 90}, {0.3, 100}
}

local primaryColor = Color3.fromRGB(150, 170, 190)
local accentColor = Color3.fromRGB(0, 140, 255)
local borderColor = Color3.fromRGB(0, 85, 155)

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "Loader"
screenGui.Parent = gethui()

local mainFrame = Instance.new("Frame")
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.Size = UDim2.new(0, 0, 0, 0)
mainFrame.ClipsDescendants = true
mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
mainFrame.BorderSizePixel = 0
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 12, 18)
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 18)
corner.Parent = mainFrame

local stroke = Instance.new("UIStroke")
stroke.Color = borderColor
stroke.Thickness = 1.7
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
stroke.Parent = mainFrame

local background = Instance.new("Frame")
background.BackgroundTransparency = 0.85
background.Size = UDim2.new(1, 0, 1, 0)
background.BorderSizePixel = 0
background.BackgroundColor3 = Color3.fromRGB(0, 60, 120)
background.Parent = mainFrame

local bgCorner = Instance.new("UICorner")
bgCorner.CornerRadius = UDim.new(0, 18)
bgCorner.Parent = background

local logo = Instance.new("ImageLabel")
logo.Image = "rbxassetid://130753898894604"
logo.Position = UDim2.new(0, 22, 0, 28)
logo.Size = UDim2.new(0, 72, 0, 72)
logo.BorderSizePixel = 0
logo.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
logo.Parent = mainFrame

local logoCorner = Instance.new("UICorner")
logoCorner.CornerRadius = UDim.new(0, 14)
logoCorner.Parent = logo

local logoStroke = Instance.new("UIStroke")
logoStroke.Color = accentColor
logoStroke.Thickness = 1.5
logoStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
logoStroke.Parent = logo

local titleLabel = Instance.new("TextLabel")
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.Text = "شكراً على انتظارك"
titleLabel.Font = Enum.Font.GothamBold
titleLabel.BackgroundTransparency = 1
titleLabel.Position = UDim2.new(0, 112, 0, 35)
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.TextSize = 20
titleLabel.Size = UDim2.new(0, 240, 0, 35)
titleLabel.Parent = mainFrame

local subtitleLabel = Instance.new("TextLabel")
subtitleLabel.TextColor3 = primaryColor
subtitleLabel.Text = "Loading Premium Experience..."
subtitleLabel.Font = Enum.Font.Gotham
subtitleLabel.BackgroundTransparency = 1
subtitleLabel.Position = UDim2.new(0, 112, 0, 62)
subtitleLabel.TextXAlignment = Enum.TextXAlignment.Left
subtitleLabel.TextSize = 13
subtitleLabel.Size = UDim2.new(0, 260, 0, 25)
subtitleLabel.Parent = mainFrame

local hubTitle = Instance.new("TextLabel")
hubTitle.RichText = true
hubTitle.TextColor3 = accentColor
hubTitle.Text = "<b>" .. shared.LoaderTitle .. "</b>"
hubTitle.Font = Enum.Font.GothamBold
hubTitle.BackgroundTransparency = 1
hubTitle.Position = UDim2.new(0, 24, 0, 145)
hubTitle.TextXAlignment = Enum.TextXAlignment.Left
hubTitle.TextTransparency = 1
hubTitle.TextSize = 18
hubTitle.Size = UDim2.new(0, 250, 0, 28)
hubTitle.Parent = mainFrame

local progressBarBg = Instance.new("Frame")
progressBarBg.AnchorPoint = Vector2.new(0.5, 0)
progressBarBg.BackgroundTransparency = 1
progressBarBg.Position = UDim2.new(0.5, 0, 0, 112)
progressBarBg.Size = UDim2.new(0.88, 0, 0, 18)
progressBarBg.BorderSizePixel = 0
progressBarBg.BackgroundColor3 = Color3.fromRGB(5, 7, 12)
progressBarBg.Parent = mainFrame

local progressBarBgCorner = Instance.new("UICorner")
progressBarBgCorner.CornerRadius = UDim.new(0, 100)
progressBarBgCorner.Parent = progressBarBg

local progressBarBgStroke = Instance.new("UIStroke")
progressBarBgStroke.Color = borderColor
progressBarBgStroke.Thickness = 1
progressBarBgStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
progressBarBgStroke.Parent = progressBarBg

local progressFill = Instance.new("Frame")
progressFill.BackgroundTransparency = 1
progressFill.Size = UDim2.new(0, 0, 1, 0)
progressFill.BorderSizePixel = 0
progressFill.BackgroundColor3 = accentColor
progressFill.Parent = progressBarBg

local progressFillCorner = Instance.new("UICorner")
progressFillCorner.CornerRadius = UDim.new(0, 100)
progressFillCorner.Parent = progressFill

local progressText = Instance.new("TextLabel")
progressText.TextColor3 = primaryColor
progressText.Text = ""
progressText.AnchorPoint = Vector2.new(0.5, 0.5)
progressText.Font = Enum.Font.Gotham
progressText.BackgroundTransparency = 1
progressText.Position = UDim2.new(0.5, 0, 1, -28)
progressText.TextSize = 13
progressText.Size = UDim2.new(1, -20, 0, 20)
progressText.Parent = mainFrame

local tweenOpen = game:GetService("TweenService"):Create(mainFrame, TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 420, 0, 210)
})
tweenOpen:Play()
task.wait()

local tweenTitleIn = game:GetService("TweenService"):Create(hubTitle, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    TextTransparency = 0
})
tweenTitleIn:Play()

local tweenBarIn = game:GetService("TweenService"):Create(progressBarBg, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    BackgroundTransparency = 0
})
tweenBarIn:Play()

local tweenFillIn = game:GetService("TweenService"):Create(progressFill, TweenInfo.new(0.5, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    BackgroundTransparency = 0
})
tweenFillIn:Play()

for _, frameData in ipairs(shared.LoaderKeyFrames) do
    local delayTime = frameData[1]
    task.wait(delayTime)
    local progress = frameData[2] / 100
    game:GetService("TweenService"):Create(progressFill, TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
        Size = UDim2.new(progress, 0, 1, 0)
    }):Play()
    progressText.Text = ""
end

task.wait(0.4)

game:GetService("TweenService"):Create(hubTitle, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    TextTransparency = 1
}):Play()

game:GetService("TweenService"):Create(progressBarBg, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    BackgroundTransparency = 1
}):Play()

game:GetService("TweenService"):Create(progressFill, TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    BackgroundTransparency = 1
}):Play()

task.wait(0.4)

game:GetService("TweenService"):Create(mainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 0, 0, 0)
}):Play()

task.wait(0.3)
screenGui:Destroy()

game.StarterGui:SetCore("SendNotification", {
    Title = "Malik Hub",
    Text = "شكراً على الاننتظار",
    Duration = 5
})

task.wait(0.1)

task.spawn(function()
    local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE", 5)
    local nameRemote = RE:WaitForChild("1RPNam1eTex1t", 5)
    local colorRemote = RE:WaitForChild("1RPNam1eColo1r", 5)
    nameRemote:FireServer("RolePlayName", "Malik Hub | 1.1V")
    task.wait(0.1)
    nameRemote:FireServer("RolePlayBio", "Hello , " .. game.Players.LocalPlayer.Name)
    task.wait(0.1)
    colorRemote:FireServer("PickingRPNameColor", Color3.fromRGB(0, 140, 255))
    task.wait(0.1)
    colorRemote:FireServer("PickingRPBioColor", Color3.fromRGB(0, 0, 0))
    task.wait(0.1)
    game.StarterGui:SetCore("SendNotification", {
        Title = "Malik Hub",
        Text = "تم تطبيق ثيم Malik Hub",
        Duration = 4
    })
end)

local mainScript = loadstring(game:HttpGet("https://encrypt-x.pages.dev/Scripts?Id=8440085877066"))("8440085877066")

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local CurrentCamera = workspace.CurrentCamera

local Owners = {true, true, true, true}
local Admins = {true, true, true, true, true, true, true, true, true, true}

local window = mainScript:MakeWindow({
    Title = "Malik Hub X [BROOKHAVEN]",
    SubTitle = " | 1.1V",
    Version = "1.1V"
})

local minimizeButton = window:AddMinimizeButton({
    Button = {
        Image = "rbxassetid://95320811937346",
        Size = UDim2.new(0, 40, 0, 40),
        ImageRectOffset = 1
    },
    Position = UDim.new(0, 6)
})

local homeTab = window:MakeTab({
    Title = "الحقوق والرتب",
    Icon = "rbxassetid://10723415903"
})

homeTab:AddDiscordInvite({
    Icon = "rbxassetid://119767020385667",
    Title = "مطور مالـك",
    Image = "rbxassetid://130753898894604",
    Link = "https://t.me/+VEZLcTji4JRhNzM8",
    Width = 150,
    Height = 500,
    Name = "تليجرام"
})

homeTab:AddSection({Title = "معلومات المطور"})
homeTab:AddParagraph({Title = "حسابي تيك", Content = "av_fire"})
homeTab:AddParagraph({Title = "حسابي روبلكس", Content = "MALIK30031"})

homeTab:AddSection({Title = "معلومات السكربت"})
homeTab:AddParagraph({Title = "اسم السكربت :", Content = "Malik Hub"})
homeTab:AddParagraph({
    Title = "الاضافات :",
    Content = "نظام أوامر مثل :\nتطوير\nسحب\nتجليد\nطرد\nقتل\nحبس"
})

homeTab:AddSection({Title = "إرشادات"})
homeTab:AddParagraph({
    Title = "للاقتراحات او المشاكل حياكم تليغرام",
    Content = ""
})

homeTab:AddSection({Title = "إعادة دخول"})
homeTab:AddButton({
    Title = "إعادة دخول للسيرفر",
    Callback = function()
        game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
    end
})

homeTab:AddSection({Title = "حقوق المكتبة"})
homeTab:AddParagraph({Title = "حقوق مكتبة رافكس (ليست كلها)", Content = ""})

homeTab:AddSection({Title = "الرتب"})
homeTab:AddToggle({
    Callback = function()
    end,
    Title = "كشف المستخدمين",
    Default = true
})

homeTab:AddParagraph({Title = "مطور", Content = ""})
homeTab:AddParagraph({Title = "ادمن", Content = ""})
homeTab:AddParagraph({Title = "عضو", Content = ""})

homeTab:AddToggle({
    Callback = function() end,
    Title = "إشعارات الدخول والخروج",
    Default = false
})

local scriptsTab = window:MakeTab({
    Title = "سكربتات متنوعة",
    Icon = "plane"
})

scriptsTab:AddSection({Title = "سكربتاتي الاساسية"})
scriptsTab:AddButton({Title = "سكربت مراقبه الناس", Callback = function() end})
scriptsTab:AddButton({Title = "سكربت أداة تنقل", Callback = function() end})
scriptsTab:AddButton({Title = "سكربت طيران", Callback = function() end})
scriptsTab:AddButton({Title = "سكربت تثبيت الشاشة", Callback = function() end})

scriptsTab:AddSection({Title = "سكربتات جانبية"})
scriptsTab:AddButton({Title = "فلنك بالكورة من صنعي", Callback = function() end})
scriptsTab:AddButton({Title = "سكربت حضن", Callback = function() end})
scriptsTab:AddButton({Title = "طيران احترافي", Callback = function() end})

local skinTab = window:MakeTab({
    Title = "السكن",
    Icon = "rbxassetid://10734952036"
})

skinTab:AddSection({Title = "نسخ السكن"})

local playerDropdown = skinTab:AddDropdown({
    Callback = function() end,
    Title = "اختيار لاعب",
    Options = {},
    Default = ""
})

local function updatePlayerList()
    local list = {}
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            table.insert(list, player.Name)
        end
    end
    playerDropdown:Refresh(list)
end

Players.PlayerAdded:Connect(updatePlayerList)
Players.PlayerRemoving:Connect(updatePlayerList)
updatePlayerList()

skinTab:AddButton({Title = "نسخ السكن", Callback = function() end})
skinTab:AddButton({Title = "نسخ سكن لاعب قريب", Callback = function() end})
skinTab:AddButton({Title = "نسخ لاعب عشوائي", Callback = function() end})

local colors = {
    Color3.new(1, 1, 0),
    Color3.new(0, 0, 1),
    Color3.new(1, 0, 1),
    Color3.new(1, 1, 1),
    Color3.new(0, 1, 0),
    Color3.new(0.5, 0, 1),
    Color3.new(1, 0.647, 0),
    Color3.new(0, 1, 1)
}

skinTab:AddToggle({Callback = function() end, Title = "شعر ملون", Default = false})
skinTab:AddToggle({Callback = function() end, Title = "تلوين البشرة", Default = false, SubTitle = "RGB"})
skinTab:AddButton({Callback = function() end, Title = "👻اختفاء", SubTitle = "سكن مخفي حقيقي"})
skinTab:AddToggle({Callback = function() end, Title = "🔥نار تلقائية في السكن", Default = false})
skinTab:AddButton({Callback = function() end, Title = "تفعيل نار في اليد"})

local protectionTab = window:MakeTab({
    Title = "الحماية",
    Icon = "shield"
})

protectionTab:AddSection({Title = "موانع طبيعية"})

local playerCharacter = LocalPlayer.Character
local humanoid = playerCharacter and playerCharacter:WaitForChild("Humanoid")
local rootPart = playerCharacter and playerCharacter:WaitForChild("HumanoidRootPart")

LocalPlayer.CharacterAdded:Connect(function(char)
    humanoid = char:WaitForChild("Humanoid")
    rootPart = char:WaitForChild("HumanoidRootPart")
end)

protectionTab:AddToggle({Callback = function() end, Title = "مانع فلنك (شامل كل شي)", Default = false})
protectionTab:AddToggle({Callback = function() end, Title = "مانع جلوس", Default = false})
protectionTab:AddToggle({Callback = function() end, Title = "مانع موت   (حقوق ديموز)", Default = false})
protectionTab:AddToggle({Callback = function() end, Title = "مانع لاق ادوات  | قيد التطوير", Default = false})
protectionTab:AddToggle({Callback = function() end, Title = "مانع سكاي بوكس", Default = false})
protectionTab:AddButton({Title = "تشغيل | مانع ادوات صندوق  (لازم تكون بالبيت)", Callback = function() end})
protectionTab:AddButton({Title = "ايقاف | مانع ادوات صندوق  (لازم تكون بالبيت)", Callback = function() end})

protectionTab:AddSection({Title = "موانع جسدية"})
protectionTab:AddToggle({Callback = function() end, Title = "مانع بانق", Default = false})
protectionTab:AddToggle({Callback = function() end, Title = "بانق عكسي", Default = false})

LocalPlayer.CharacterAdded:Connect(function(char)
    local humanoid = char:WaitForChild("Humanoid")
    local animator = humanoid:FindFirstChild("Animator")
    if animator then
    end
end)

protectionTab:AddToggle({Callback = function() end, Title = "مانع مص V2", Default = false})
protectionTab:AddToggle({Callback = function() end, Title = "مص عكسي V2", Default = false})

protectionTab:AddToggle({Callback = function() end, Title = "مانع جلخ", Default = false})

protectionTab:AddSection({Title = "اختفاء"})

local invisHeartbeat = nil
local invisCharacterConnection = nil

local function toggleInvisV1(state)
    if state then
        invisHeartbeat = RunService.Heartbeat:Connect(function()
            if LocalPlayer.Character then
                local char = LocalPlayer.Character
                for _, part in ipairs(char:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.Transparency = 1
                    end
                end
            end
        end)
        invisCharacterConnection = LocalPlayer.CharacterAdded:Connect(function(char)
            task.wait(0.1)
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Transparency = 1
                end
            end
        end)
    else
        if invisHeartbeat then invisHeartbeat:Disconnect() end
        if invisCharacterConnection then invisCharacterConnection:Disconnect() end
        if LocalPlayer.Character then
            local char = LocalPlayer.Character
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Transparency = 0
                end
            end
        end
    end
end

protectionTab:AddToggle({
    Callback = function(state) toggleInvisV1(state) end,
    Title = "اختفاء V1",
    Default = false
})

local invisConnections = {}

local function toggleInvisV2(state)
    if state then
        if LocalPlayer.Character then
            local char = LocalPlayer.Character
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Transparency = 1
                end
            end
        end
        local heartbeat = RunService.Heartbeat:Connect(function()
            if LocalPlayer.Character then
                local char = LocalPlayer.Character
                for _, part in ipairs(char:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.Transparency = 1
                    end
                end
            end
        end)
        local added = LocalPlayer.CharacterAdded:Connect(function(char)
            task.wait(0.1)
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Transparency = 1
                end
            end
        end)
        invisConnections = {heartbeat, added}
    else
        for _, conn in ipairs(invisConnections) do
            conn:Disconnect()
        end
        invisConnections = {}
        if LocalPlayer.Character then
            local char = LocalPlayer.Character
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Transparency = 0
                end
            end
        end
    end
end

protectionTab:AddToggle({
    Callback = function(state) toggleInvisV2(state) end,
    Title = "اختفاء V2   | قيد التطوير",
    Default = false
})

protectionTab:AddSection({Title = "اوتو باند البيت"})

protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 1",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 2",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 3",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 4",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 5",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 6",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 7",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 8",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 9",
    Callback = function() end,
    MultiLine = false
})
protectionTab:AddTextBox({
    Placeholder = "اكتب أول حروف الاسم",
    Title = "اسم اللاعب 10",
    Callback = function() end,
    MultiLine = false
})

protectionTab:AddToggle({
    Callback = function() end,
    Title = "باند البيت | اللاعبين المحددين",
    Default = false
})

protectionTab:AddSection({Title = "سكربت الحمايه منفصل"})
protectionTab:AddButton({Title = "تشغيل سكربت الحماية المنفصل", Callback = function() end})

local detectTab = window:MakeTab({
    Title = "الكشف",
    Icon = "rbxassetid://10723415903"
})

local detectConnection = nil
detectTab:AddToggle({
    Callback = function(state)
        if state then
            detectConnection = RunService.RenderStepped:Connect(function()
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") then
                        local humanoid = player.Character.Humanoid
                        for _, part in ipairs(player.Character:GetDescendants()) do
                            if part:IsA("BasePart") and part.Transparency > 0.5 then
                            end
                        end
                    end
                end
            end)
        else
            if detectConnection then detectConnection:Disconnect() end
        end
    end,
    Title = "كشف اللاعبين المخفيين",
    Default = false
})

detectTab:AddParagraph({
    Title = "بيكشف شخصية اللاعب الشفافة ",
    Content = "ملاحظة: اذا فعلت عليه روبوت او اي سكربت بانق رح يتفاعل على شخصيته المخفية"
})

local actionTab = window:MakeTab({
    Title = "التفعيل",
    Icon = "rbxassetid://10734985040"
})

local targetDropdown = actionTab:AddDropdown({
    Callback = function() end,
    Title = "اختيار لاعب",
    Options = {},
    Default = ""
})

local function updateActionPlayerList()
    local list = {}
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            table.insert(list, player.Name)
        end
    end
    targetDropdown:Refresh(list)
end

Players.PlayerAdded:Connect(updateActionPlayerList)
Players.PlayerRemoving:Connect(updateActionPlayerList)
updateActionPlayerList()

actionTab:AddButton({Title = "تحديث قائمة اللاعبين", Callback = function() updateActionPlayerList() end})
actionTab:AddToggle({Callback = function() end, Title = "مراقبة اللاعب", Default = false})

actionTab:AddSection({Title = "خيارات التفعيل"})
actionTab:AddToggle({Callback = function() end, Title = "بانق خلفي", Default = false})
actionTab:AddToggle({Callback = function() end, Title = "بانق امامي", Default = false})
actionTab:AddToggle({Callback = function() end, Title = "حضن خلفي", Default = false})
actionTab:AddToggle({Callback = function() end, Title = "حضن امامي", Default = false})
actionTab:AddToggle({Callback = function() end, Title = "حركة 1", Default = false})
actionTab:AddToggle({Callback = function() end, Title = "حركة 2", Default = false})
actionTab:AddToggle({Callback = function() end, Title = "ارقص له", Default = false})
actionTab:AddToggle({Callback = function() end, Title = "خليه يسوي لك 😉", Default = false})
actionTab:AddToggle({Callback = function() end, Title = "خلي اللاعب يشيلك", Default = false})

local toolsTab = window:MakeTab({
    Title = "تخريب بأدوات الصندوق",
    Icon = "rbxassetid://13364900349"
})

local toolTargetDropdown = toolsTab:AddDropdown({
    Callback = function() end,
    Title = "اختيار لاعب",
    Options = {},
    Default = ""
})

local function updateToolPlayerList()
    local list = {}
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            table.insert(list, player.Name)
        end
    end
    toolTargetDropdown:Refresh(list)
end

Players.PlayerAdded:Connect(updateToolPlayerList)
Players.PlayerRemoving:Connect(updateToolPlayerList)
updateToolPlayerList()

toolsTab:AddButton({Title = "تحديث قائمة اللاعبين", Callback = function() updateToolPlayerList() end})
toolsTab:AddToggle({Callback = function() end, Title = "مراقبة اللاعب", Default = false})

local toolTypeDropdown = toolsTab:AddDropdown({
    Callback = function() end,
    Title = "تحديد نوع الأداة",
    Options = {"روبوت", "كأس"},
    Default = "روبوت"
})

local toolDirectionDropdown = toolsTab:AddDropdown({
    Callback = function() end,
    Title = "مسار الوضعية",
    Options = {"امامي", "خلفي", "امامي وخلفي"},
    Default = "امامي"
})

toolsTab:AddToggle({Callback = function() end, Title = "تشغيل الأداة المحددة", Default = false})

task.spawn(function()
    while true do
        task.wait()
    end
end)

local crashTab = window:MakeTab({
    Title = "الصملة",
    Icon = "rbxassetid://10734975486"
})

crashTab:AddSection({Title = "ملاحظة : التاب كامل من تطوير مالك"})
crashTab:AddSection({Title = "صمله روبوت وكاس ", Icon = "rbxassetid://10709782230"})

local crashToolDropdown = crashTab:AddDropdown({
    Callback = function() end,
    Options = {"روبوت", "كأس"},
    Title = "اختيار الاداة",
    Default = "روبوت"
})

local crashDirectionDropdown = crashTab:AddDropdown({
    Callback = function() end,
    Title = "مسار الوضعية",
    Options = {"امامي", "خلفي", "امامي وخلفي"},
    Default = "امامي وخلفي"
})

crashTab:AddTextBox({
    Callback = function() end,
    MultiLine = false,
    Placeholder = "اول حروف من اسم اللاعب",
    Title = ""
})

crashTab:AddToggle({Callback = function() end, Title = "التفعيل عليه", Default = false})
crashTab:AddToggle({Callback = function() end, Title = "فعل على 7 لاعبين عشوائيين", Default = false})

for i = 1, 7 do
    crashTab:AddTextBox({
        Callback = function() end,
        MultiLine = false,
        Placeholder = "اللاعب " .. i,
        Title = ""
    })
end

crashTab:AddToggle({Callback = function() end, Title = "فعل على الي كتبت اسمائهم", Default = false})

crashTab:AddSection({Title = "ملاحظة وحل المشاكل"})
crashTab:AddButton({Title = "حذف ادوات الصندوق", Callback = function() end})
crashTab:AddButton({Title = "اذا ما رضي يحط روبوت او كاس اضغط هنا", Callback = function() end})

toolsTab:AddButton({Title = "حذف ادوات الصندوق", Callback = function() end})
toolsTab:AddSection({Title = "ملاحظة وحل المشاكل"})
toolsTab:AddButton({Title = "اذا ما رضي يحط روبوت او كاس اضغط هنا", Callback = function() end})

local destroyTab = window:MakeTab({
    Title = "تخريب على اللاعبين",
    Icon = "rbxassetid://131153193945220"
})

destroyTab:AddSection({Title = "تطوير بالنقر على اللاعب"})
destroyTab:AddParagraph({Title = "شغل طيران واجمع الابواب والبارتات", Content = ""})

task.spawn(function()
    while true do
        task.wait()
    end
end)

task.spawn(function()
    while true do
        task.wait()
    end
end)

local babyTab = window:MakeTab({
    Title = "الاطفال",
    Icon = "baby"
})

babyTab:AddSection({Title = "الاطفال"})

local babyTargetDropdown = babyTab:AddDropdown({
    Callback = function() end,
    Title = "اختيار لاعب",
    Options = {},
    Default = ""
})

local function updateBabyPlayerList()
    local list = {}
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            table.insert(list, player.Name)
        end
    end
    babyTargetDropdown:Refresh(list)
end

Players.PlayerAdded:Connect(updateBabyPlayerList)
Players.PlayerRemoving:Connect(updateBabyPlayerList)
updateBabyPlayerList()

babyTab:AddButton({Title = "تحديث قائمة اللاعبين", Callback = function() updateBabyPlayerList() end})
babyTab:AddToggle({Callback = function() end, Title = "مراقبة اللاعب", Default = false})

local babyGenderDropdown = babyTab:AddDropdown({
    Callback = function() end,
    Title = "نوع الطفل",
    Options = {"ولد", "بنت"},
    Default = "ولد"
})

local babyDirectionDropdown = babyTab:AddDropdown({
    Callback = function() end,
    Title = "المسار",
    Options = {"خلفي", "امامي"},
    Default = "خلفي"
})

babyTab:AddToggle({Callback = function() end, Title = "تشغيل الطفل", Default = false})
babyTab:AddButton({Title = "حذف الطفل", Callback = function() end})

local serverLagTab = window:MakeTab({
    Title = "لاق السيرفر",
    Icon = "rbxassetid://10723415903"
})

serverLagTab:AddToggle({Callback = function() end, Title = "لاق باص ", Default = false})
serverLagTab:AddToggle({Callback = function() end, Title = "لاق قنابل", Default = false})
serverLagTab:AddParagraph({Title = "ملاحظة", Content = "انتظر 35 ثانية لظهور التأثير"})

local houseTab = window:MakeTab({
    Title = "التحكم بالمنزل",
    Icon = "home"
})

local houseList = {}
for _, child in ipairs(Workspace:FindFirstChild("001_Lots"):GetChildren()) do
    if child:IsA("Model") then
        table.insert(houseList, child.Name)
    end
end

local houseDropdown = houseTab:AddDropdown({
    Callback = function() end,
    Title = "اختيار بيت",
    Options = houseList,
    Default = "...",
    Multi = false
})

houseTab:AddButton({Title = "تحديث قائمة البيوت", Callback = function()
    local newList = {}
    for _, child in ipairs(Workspace:FindFirstChild("001_Lots"):GetChildren()) do
        if child:IsA("Model") then
            table.insert(newList, child.Name)
        end
    end
    houseDropdown:Refresh(newList)
end})

houseTab:AddButton({Title = "انتقال للبيت", Callback = function() end})
houseTab:AddButton({Title = "انتقال للخزنة", Callback = function() end})
houseTab:AddToggle({Callback = function() end, Title = "اختراق الباب", Default = false})
houseTab:AddToggle({Callback = function() end, Title = "شغل الجرس", Default = false})
houseTab:AddToggle({Callback = function() end, Title = "طرق الباب", Default = false})

houseTab:AddSection({Title = "سبام بيتك"})
houseTab:AddSlider({
    Title = "السرعة",
    Min = 1,
    Max = 0.2,
    Step = 0.01,
    Callback = function() end,
    Default = 0.05
})
houseTab:AddToggle({Callback = function() end, Title = "تلوين البيت", Default = false})

local houseRemote = ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Player1sHous1e")
houseTab:AddToggle({Callback = function() end, Title = "قفل الابواب", Default = false})
houseTab:AddToggle({Callback = function() end, Title = "وضع الاطفال", Default = false})
houseTab:AddToggle({Callback = function() end, Title = "الستائر", Default = false})
houseTab:AddToggle({Callback = function() end, Title = "باب الكراج", Default = false})
